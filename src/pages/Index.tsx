import { LevelProgressDemo } from "@/components/dashboard/LevelProgressDemo";

const Index = () => {
  // Show the level progress demo for now
  return <LevelProgressDemo />;
};

export default Index;
